<div class='jumbotron'>
  <h1>Quick Notes</h1>
</div>
<div class='container'>
  <% @notes.each do |note|%>
  <div class='notes-container'>
    <h3><%= note.title %></h3>
    <p> <%= note.content %> </p>
    <% note.comments.each do |comment| %>
      <p><%= comment.title %></p>
    <% end %>
  </div>
  <% end %>
</div>
